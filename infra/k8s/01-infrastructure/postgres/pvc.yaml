apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: threatsense
spec:
  storageClassName: nfs
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 50Gi
