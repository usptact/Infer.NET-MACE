apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mqtt-data
  namespace: threatsense
spec:
  storageClassName: nfs
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi
