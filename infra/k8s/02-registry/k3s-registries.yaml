# Copy this file to /etc/rancher/k3s/registries.yaml on EVERY k3s node,
# then restart k3s: systemctl restart k3s  (or k3s-agent on workers)
mirrors:
  "192.168.10.201:5000":
    endpoint:
      - "http://192.168.10.201:5000"
