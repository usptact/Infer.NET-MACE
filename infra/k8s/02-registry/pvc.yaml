apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-data
  namespace: threatsense
spec:
  storageClassName: nfs
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 20Gi
