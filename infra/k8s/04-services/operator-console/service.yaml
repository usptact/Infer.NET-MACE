apiVersion: v1
kind: Service
metadata:
  name: operator-console
  namespace: threatsense
spec:
  type: ClusterIP
  selector:
    app: operator-console
  ports:
  - name: http
    port: 80
    targetPort: 80
